# Haus & Grace tenant isolation

- This repository is **Haus & Grace only** (`hausandgrace.ae` / Worker `haus-grace-primary`).
- Do **not** add Espacios interactive map routes (`/map`) or Espacios map data bindings.
- Public catalogue may arrive only via Espacios release records; private PSR/Espacios data must not land here.
- Campaign/email/LeadRat stay disabled until H&G credentials are provisioned.
